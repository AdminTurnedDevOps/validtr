# `validtr help`

Show command help and usage details.

## Usage

```bash
validtr help
validtr help run
validtr run --help
```

Use this to inspect flags and examples directly from the installed binary.
