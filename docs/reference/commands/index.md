# CLI Commands

This section documents each available CLI command in detail.

## Available Commands

- `completion`: generate shell completion scripts.
- `config`: manage local non-secret settings.
- `help`: show command help.
- `mcp`: discover MCP servers.
- `run`: execute and validate tasks.

## Global Flag

All commands support:

- `-c, --config`: custom config file path.
